import PaintElement from "../../PaintElement";

/**
 * Temporary mutation performed on PaintScene via PaintManager.
 * Used to make things like tooling work.
 */
export default abstract class PaintManagerMutation {
}