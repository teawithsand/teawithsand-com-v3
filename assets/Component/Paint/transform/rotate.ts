import { Point } from "../primitive";

export const makeRotateTranslator = (origin: Point, angleRadian: number) => {
    return (point: Point) => {
        console.error("rotation of points is NIY")

        // TODO(teawithsand): implement it
        return point
    }
}