/**
 * Interface, which is implemented by data of all PaintElements.
 */
export default interface PaintElementData {
	// CSS filters to be applied to given element.
	filters: PaintElementFilter[]
}

export type PaintElementFilter = {
	// NIY definition
}
